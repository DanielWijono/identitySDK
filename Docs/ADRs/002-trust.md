# ADR 002: Host and provider own trust decisions

Accepted. The host obtains scoped credentials and disclosure copy. The provider persists consent and decides verification. The SDK orchestrates confirmed evidence delivery. Host and SDK share a process trust boundary. Public token access is necessary but descriptions are redacted. Synthetic decisions belong only in optional demo support. Provider messages are never forwarded as arbitrary errors.
