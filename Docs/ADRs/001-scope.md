# ADR 001: Source-first, narrowly scoped package

Accepted for the foundation. Publish source-based Swift Package products for core and explicitly selected demo support. iOS 16 is the intended product minimum; macOS 13 enables core tests. Real capture, encrypted storage and UI receive separate modules only when implemented. No authenticity, liveness, NFC or certification claims. Binary distribution is deferred.
